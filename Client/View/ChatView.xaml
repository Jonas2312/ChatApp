<UserControl x:Class="Client.View.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="300"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <TextBox Grid.Row="1" x:Name="TextBox" Text="{Binding MessageDraft}"></TextBox>
        <Button Grid.Row="2" x:Name="SendMessage" Content="Send Message" Command="{Binding SendMessageCommand}"></Button>

        <Button Grid.Column="1" Grid.Row="1" x:Name="UploadFile" Content="UploadFile" Command="{Binding UploadFileCommand}"></Button>
        <Button Grid.Column="1" Grid.Row="2" x:Name="DownloadFile" Content="DownloadFile" Command="{Binding DownloadFileCommand}"></Button>

    </Grid>
</UserControl>
